apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudops

resources:
  - namespace.yaml
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: ghcr.io/wyhffw/cloudops-backend
    newTag: latest
