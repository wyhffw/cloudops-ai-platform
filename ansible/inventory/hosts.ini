# 按你的实际 IP / SSH 用户修改
# 推荐在 master 上当控制节点执行 ansible

[masters]
master ansible_host=192.168.1.20

[workers]
worker1 ansible_host=192.168.1.21

[k8s:children]
masters
workers

[k8s:vars]
ansible_user=root
# 若用普通用户 + sudo，改成：
# ansible_user=ubuntu
# ansible_become=true
